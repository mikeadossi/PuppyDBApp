extends layout

block content
  h1 Find A Puppy

  div.searchHeadingsTable
    ul
      for q in query
        li=q.name
        li=q.description
        li=q.breed
        li
          img(src=q.image_url)
